# v1

Oldest compatible JSON shapes and prompt fragments. Use when backfilling or validating rows that predate Gold Standard.

Optional files (add as you extract):

- `schema.ts` — Zod / types
- `prompt.ts` — prompt hash + doc refs
- `validator.ts`
- `template.ts` — renderer hints
