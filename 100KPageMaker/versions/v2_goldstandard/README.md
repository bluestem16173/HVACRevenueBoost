# v2 Gold Standard

Aligns with **`schema_version: "v2_goldstandard"`** and `components/gold/GoldStandardPage.tsx`.

Optional files:

- `schema.ts`
- `prompt.ts` — see `docs/` + `lib/content-engine/core.ts`
- `validator.ts`
- `template.ts`
