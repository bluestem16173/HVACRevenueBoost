/** Substrings that must not appear in stored JSON (UI / prompt scaffolding). */
export const HSD_SCAFFOLDING_SUBSTRINGS = [
  "Immediate clarity",
  "No fluff",
  "Flowchart view is temporarily disabled",
] as const;
