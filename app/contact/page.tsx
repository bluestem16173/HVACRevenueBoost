import LeadCard from "@/components/LeadCard";

export default function ContactPage() {
  return (
    <main className="min-h-[80vh] flex flex-col items-center justify-center py-12 px-4 bg-slate-50">
      <LeadCard />
    </main>
  );
}
