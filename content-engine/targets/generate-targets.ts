import type { PageTarget } from "@/lib/content-system/page-target-types";

/** Placeholder — replace with graph/SEO query that emits PageTarget[] */
export function generateTargetsFromSeed(_opts: { limit?: number }): PageTarget[] {
  return [];
}
