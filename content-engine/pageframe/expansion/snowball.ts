/**
 * Snowball / hub expansion — see docs/MASTER-PROMPT-AUTHORITY-HUB-SNOWBALL.md
 * Wire to graph linker when ready.
 */
export function snowballRelatedSlugs(_seed: string): string[] {
  return [];
}
