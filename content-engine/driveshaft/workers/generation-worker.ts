/**
 * Production worker lives at `scripts/generation-worker.ts` (tsx entry).
 * Import that path from automation; this module documents the link.
 */
export const GENERATION_WORKER_ENTRY = "scripts/generation-worker.ts";
